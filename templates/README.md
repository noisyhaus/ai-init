# __PROJECT_NAME__

## Purpose
- Fill in what this project does.

## Quick Start
- Install:
- Dev:
- Test:
- Lint:
- Build:

## Notes
- `AGENTS.md` defines the working protocol for Codex CLI in this repo.
- `docs/ai/project_memory.md` holds long-lived project structure and canonical sources.
- `docs/ai/change_playbook.md` holds change checklists and verification expectations.
- `docs/ai/current_state.md` is the working checkpoint for the next session.
- `docs/ai/agent_memory.md` and `docs/ai/user_preferences.md` are optional durable memory layers.
- If `.ai-index/` exists later, treat it as a recall cache only. Markdown docs remain the source of truth.
- Existing projects can adopt the same structure incrementally.
